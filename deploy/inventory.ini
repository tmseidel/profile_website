# Ansible inventory for profile_website deployment
#
# This file is not used directly. Use deploy.sh which generates a
# temporary inventory from your .env file.
#
# If you prefer to use this file directly, fill in the values below:

[webservers]
# webserver ansible_host=192.0.2.1 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[webservers:vars]
# server_name=example.com
