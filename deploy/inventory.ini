# Ansible inventory for profile_website deployment
# Copy this file, fill in your server details, and run:
#   ansible-playbook -i inventory.ini playbook.yml

[webservers]
# Replace with your server's IP or hostname:
# webserver ansible_host=192.0.2.1 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[webservers:vars]
# Override the nginx server_name (domain name or IP):
# server_name=example.com
